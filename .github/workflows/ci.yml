name: CI

on:
  push:
    branches: ["master", "main"]
  pull_request:
    branches: ["master", "main"]

jobs:
  frontend:
    name: Frontend tests (Vitest)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: src/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: src

      - name: Run Vitest
        run: npx vitest run
        working-directory: src

  python:
    name: Python sidecar tests (pytest)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: src-tauri/sidecar/requirements.txt

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r src-tauri/sidecar/requirements.txt pytest

      - name: Run pytest
        # No PYTHONPATH is inherited on the runner, so plain pytest is correct here.
        # (Locally, `env -u PYTHONPATH` is used instead to clear a conflicting shell PYTHONPATH.)
        run: pytest src-tauri/tests/ -q

  rust:
    name: Rust tests (cargo test)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Run cargo test
        run: cargo test --manifest-path src-tauri/Cargo.toml
